name: Keep Streamlit Alive
on:
  schedule:
    - cron: '0 * * * *' # every hour
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Ping
        run: curl -s https://flinders-uni-stantec.streamlit.app/ > /dev/null
